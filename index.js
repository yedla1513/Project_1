import React from 'react';
import ReactDOM from 'react-dom/client';
import EliteOrganizers from './EliteOrganizers.jsx';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <EliteOrganizers />
  </React.StrictMode>
);
